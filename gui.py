import tkinter as tk

root = tk.Tk()
root.title("CPU Scheduling Analysis Tool")
root.geometry("800x600")

label = tk.Label(root, text="CPU Scheduling Tool", font=("Arial", 18))
label.pack(pady=20)

root.mainloop()